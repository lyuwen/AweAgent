# Volcengine Ark LLM configuration (with extended thinking)
backend: ark
base_url: ${ARK_BASE_URL}
api_key: ${ARK_API_KEY}
model: ${ARK_MODEL_ID}
params:
  # temperature: 1.0
  max_tokens: 16384
thinking: true
# stop:
  # - "<DONE>" #
